# Simple java app demo using GraalVM's Native Image Generation

To build native image run the following command:

```
mvn package
```

then start native image:

```
./target/native-java-helloworld-demo
```
